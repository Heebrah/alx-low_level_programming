#include "main.h"
/**
 * _islower - function to check lowercase
 * @c: parameter
 * Return: allow success
 */
int _islower(int c)
{

	if (c >= 'a' && c <= 'z')
{
	return (1);
	}
	else
		return (0);
}
