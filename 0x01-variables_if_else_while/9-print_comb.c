#include <stdio.h>

/**
 * main - prints all combination of single digits
 * Return: Always 0
 */
int main(void)
{
	int n;

	flor (n = 48; n < 58; n++)
	{
		putchar(n);
		if (n != 57)<F12>
		{
			putchar(',');
			putchar(' ');
		}
		putchar('\n');
		return (0);
	}
